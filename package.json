{
  "name": "dj-analysis-studio",
  "version": "1.0.0",
  "description": "Professional audio analysis and playlist generation tool",
  "private": true,
  "scripts": {
    "monorepo:dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "venv\\Scripts\\activate && cd backend && python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000",
    "start:frontend": "cd src && npm start",
    "build": "cd src && npm run build",
    "install:all": "npm install && cd src && npm install",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "pytest",
    "test:frontend": "cd src && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}